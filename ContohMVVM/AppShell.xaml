<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ContohMVVM.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:view="clr-namespace:ContohMVVM.Views"
     FlyoutIcon="moon.png">

    

    <FlyoutItem Title="Master Vendor" Icon="moon.png">
        <ShellContent Title="Calc App" Icon="moon.png"
                      ContentTemplate="{DataTemplate view:CalcView}" />
         <ShellContent Title="Command" Icon="sun.png"
                      ContentTemplate="{DataTemplate view:CommandView}" />
    </FlyoutItem>

    <FlyoutItem Title="Customer" Icon="question.png">
        <ShellContent Title="Customer"
                      ContentTemplate="{DataTemplate view:CustomerView}" />
    </FlyoutItem>


</Shell>

